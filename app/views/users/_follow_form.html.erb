<% unless current_user? @user %>
  <div id="follow_form">
    <% if current_user.following? @user %>
      <%= render Settings.choices.unfollow %>
    <% else %>
      <%= render Settings.choices.follow %>
    <% end %>
  </div>
<% end %>
